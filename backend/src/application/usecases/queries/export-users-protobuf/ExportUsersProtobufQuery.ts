/**
 * Export Users Protobuf Query
 * Represents the intent to export all users in Protocol Buffer format
 */
export class ExportUsersProtobufQuery {
  /**
   * Creates a new ExportUsersProtobufQuery
   * No parameters needed for this query
   */
  constructor() {}
}
