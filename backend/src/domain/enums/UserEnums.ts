/**
 * User Role Enum
 * Defines the possible roles a user can have in the system
 */
export enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
  GUEST = 'guest'
}

/**
 * User Status Enum
 * Defines the possible statuses a user can have
 */
export enum UserStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive'
}
